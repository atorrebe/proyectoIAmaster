version: "3.9"

services: api:
build:
context: .
dockerfile: environment/Dockerfile ports:
- "8000:8000"
volumes:
./models:/app/models

./data:/app/data environment:
PYTHONUNBUFFERED=1
command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000
  